# Carpeta de CVs

Coloca aquí los archivos PDF de los CVs de los desarrolladores.

El archivo debe tener el nombre que corresponda con la ruta especificada en `developers.json`.

Por ejemplo, para `oswaldo-teran-cv.pdf`, el archivo debe llamarse exactamente así.

