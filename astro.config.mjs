// @ts-check
import { defineConfig } from 'astro/config';

// Esto es para el desarrollo local
// export default defineConfig({});

// Esto es para el despliegue en GitHub Pages
export default defineConfig({
  site: 'https://obababaresco.github.io',
  base: '/Pixelhada',
});
